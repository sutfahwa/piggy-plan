{
  "name": "piggy-plan",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Piggy Plan · วางแผนการเงินส่วนตัว (personal finance, tax, OT & PVD planner)",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "emu": "npx -y firebase-tools emulators:start --only auth,firestore",
    "android:icons": "node scripts/gen-icons.mjs && capacitor-assets generate --android",
    "android:sync": "vite build && cap sync android",
    "android:open": "vite build && cap sync android && cap open android",
    "android:apk": "vite build && cap sync android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor-community/sqlite": "^7.0.3",
    "@capacitor/android": "^7.6.6",
    "@capacitor/core": "^7.6.6",
    "firebase": "^12.15.0",
    "jeep-sqlite": "^2.8.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^7.6.6",
    "@vitejs/plugin-react": "^4.3.4",
    "sharp": "^0.35.1",
    "vite": "^5.4.11",
    "vite-plugin-pwa": "^1.3.0"
  }
}
