node_modules
dist
.DS_Store
*.local
.vite

# Firebase config (real keys) — keep .env.example committed
.env
.env.*
!.env.example

# Firebase emulator logs / local data
firebase-debug.log
firestore-debug.log
ui-debug.log
.firebase/
